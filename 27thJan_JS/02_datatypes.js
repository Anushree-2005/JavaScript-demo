"use strict";//treat all JS cose as newer version 

// alert(3+3)  //we  are using node JS not browser hence this will not work as it works in browser

console.log(3
    +
    3);//code credibility should ne high

    console.log("Anu");

    let sname="Anu";
    let age=18
    let isLoggedIn=false
    let state;

    console.log(typeof sname);
    console.log(typeof state);
    